// /* - закомментировать строку от...
// */ - закомментировать строку до...
// ./ - файлы находятся в одной папке

/*
import{
    ...
} from './smth'

export {
    ...
}


объект - это всё что есть или всё что есть до точки
console.dir - позволяет узнать свойства объекта
console.table - свойства объекта в табличном виде
let - объявление переменной
const = константа
delete object.option - удаляет свойство
object[variable] - передаёт значение переменной в виде ключа для объекта
JSON.parse - конвертация JSON в объект
JSON.stringify - конвертация объекта в JSON
... - оператор разделения объекта на свойства
`${}` - шаблонная строка
; - показывает инструкцию(инструкции не могут быть переданы как аргументы)
Object.assign({}||[],source) - создаёт новую ссылку на объект
array.push (JS) = array.append (python)
array.pop() - удаляет и возвращает последний элемент массива переменной если такая есть
array.unshift() - добавляет элемент в начало массива
array.shift() - удаляет и возвращает первый элемент массива переменной если такая есть
array.forEach((element,index) => function) - функция которая перебирает все элементы массива
и для каждого элемента массива будет вызывать function
array.map((element,index) => function) - функция которая перебирает все элементы массива
и создаёт новый массив на основе function без изменения основного массива
switch(expression){
    case n:
        // чё нибудь
        break
}
condition
 ? expression1(True)
 : expression2(False)
for(key in Object) - даёт ключ объекта
for(element in Array) = for element in Array(python)
for(letter of Iterable) - выводит каждый символ по отдельности
instance instanceof Object - проверяет является ли экземпляр экземпляром какого либо класса
class ...{
    constructor(...){
        this. ...
    }
    method(...){

    }
    static method(...){

    }
}



*/