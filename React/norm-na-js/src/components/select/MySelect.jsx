import React from "react";

const MySelect = ({ option }) => {
  return (
    <select>
      <option value="value1">{}</option>
      <option value="value1"></option>
    </select>
  );
};

export default MySelect;
